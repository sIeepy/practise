= form_for [movie, rate] do |f|
  = f.hidden_field :movie_id
  = f.hidden_field :user_id
  = f.text_area :rating, cols: 50, rows: 5, placeholder: "Write new comment"
  %br
  = f.submit 'Create rate', class: 'btn btn-default'
