= form_for [movie, comment] do |f|
  = f.hidden_field :movie_id
  = f.hidden_field :user_id
  = f.text_area :text, cols: 50, rows: 5, placeholder: "Write new comment"
  %br
  = f.submit 'Create comment', class: 'btn btn-default'
